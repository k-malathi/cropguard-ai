kind = "web"
previewPath = "/"
title = "CropGuard AI"
description = "A polished hackathon prototype demonstrating AI crop detection, risk assessment, autonomous decision support, localized containment, and farmer alerts through a guided interactive demo."
version = "1.0.0"
id = "artifacts/cropguard-ai"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24037

[services.development]
run = "pnpm --filter @workspace/cropguard-ai run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cropguard-ai", "run", "build" ]
serve = "static"
publicDir = "artifacts/cropguard-ai/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24037"
BASE_PATH = "/"
